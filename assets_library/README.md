# Shared Asset Library
Place reusable assets here — versioned, not per-film.

- `characters/base_rig.blend` — Rigify chibi template
- `sets/` — modular sets
- `hdris/` — download from polyhaven.com (studio_small_03)

Factory scripts symlink/copy from here into films/<name>/scenes via bpy.
